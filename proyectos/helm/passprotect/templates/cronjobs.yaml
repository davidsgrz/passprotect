apiVersion: batch/v1
kind: CronJob
metadata:
  name: security-audit
  namespace: monitoring
  labels:
    {{- include "passprotect.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.monitoring.audit.schedule | quote }}
  jobTemplate:
    spec:
      template:
        spec:
          securityContext:
            {{- include "passprotect.podSecurityContext" . | nindent 12 }}
          containers:
            - name: audit
              image: "{{ .Values.global.imageRegistry }}/ubseguridadd:latest"
              command: ["/bin/bash", "-c", "echo '=== Auditoria $(date) ===' && cat /root/logs/security_audit.log 2>/dev/null | tail -100"]
              securityContext:
                {{- include "passprotect.containerSecurityContext" . | nindent 16 }}
              resources:
                requests:
                  cpu: 50m
                  memory: 64Mi
                limits:
                  cpu: 200m
                  memory: 256Mi
          restartPolicy: OnFailure
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: vaultwarden-backup
  namespace: monitoring
  labels:
    {{- include "passprotect.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.monitoring.backup.schedule | quote }}
  jobTemplate:
    spec:
      template:
        spec:
          securityContext:
            runAsNonRoot: true
            runAsUser: 70
            fsGroup: 70
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: backup
              image: "{{ .Values.postgresVw.image.repository }}:{{ .Values.postgresVw.image.tag }}"
              command:
                - /bin/sh
                - -c
                - |
                  BACKUP_FILE="/backup/vaultwarden-$(date +%Y%m%d-%H%M%S).sql.gz"
                  pg_dump -h postgres-vw.vaultwarden.svc.cluster.local \
                    -U "${POSTGRES_USER}" \
                    -d "${POSTGRES_DB}" \
                    --no-owner --no-privileges | gzip > "$BACKUP_FILE"
                  find /backup -name "*.sql.gz" -mtime +{{ .Values.monitoring.backup.retentionDays }} -delete
              env:
                - name: POSTGRES_USER
                  value: {{ .Values.postgresVw.user | quote }}
                - name: POSTGRES_DB
                  value: {{ .Values.postgresVw.database | quote }}
                - name: PGPASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: vaultwarden-secrets
                      key: db-password
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop: [ALL]
              resources:
                requests:
                  cpu: 100m
                  memory: 128Mi
                limits:
                  cpu: 500m
                  memory: 512Mi
              volumeMounts:
                - name: backup
                  mountPath: /backup
          restartPolicy: OnFailure
          volumes:
            - name: backup
              persistentVolumeClaim:
                claimName: backup-storage
